{
  "private": true,
  "scripts": {
    "dev:server": "pnpm --filter ztyle-server dev",
    "dev:client": "pnpm --filter ztyle-client dev",
    "build:server": "pnpm --filter ztyle-server build",
    "build:client": "pnpm --filter ztyle-client build",
    "build": "pnpm build:server && pnpm build:client"
  }
}